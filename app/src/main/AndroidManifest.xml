<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.entrepreneurlist.schooldatamanagementproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".schoolLogin"></activity>
        <activity android:name=".studentResulsts" />
        <activity
            android:name=".ui.login.LoginActivitySchool"
            android:label="@string/title_activity_login_school" />
        <activity android:name=".RegisterAsStudent" />
        <activity android:name=".RegisterAsParent" />
        <activity android:name=".studentProfileActivity$RegisterSchool" />
        <activity android:name=".JoinActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".studentProfileActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>